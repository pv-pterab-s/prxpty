return if not process.env.DBG?

process.stdin.on = (event, cb) ->
  ## if event is 'data'
  ##
  ## else
  ##   process.stdin.on event, cb

M = (s) -> console.log s; s

poo = (...std) ->
  M ...std

poo 1,2,3
M 'here'
